-- =========================================================================
-- seeds.sql
-- Seeds (dados iniciais).
-- Estado ATUAL do banco para esta entidade.
-- =========================================================================

-- O banco não tem dado inicial: tudo nasce do uso. A seed de DEMONSTRAÇÃO de um
-- perfil (árvore de categorias, gastos e receitas do período) fica em
-- `supabase/seeds/demo-perfil-1.sql` e de propósito NÃO entra no full_schema.sql
-- — recriar o banco não pode trazer junto o extrato fictício de ninguém.
